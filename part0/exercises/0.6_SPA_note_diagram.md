sequenceDiagram
    participant Browser
    participant Server
    participant Database

    Browser->>Server : POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note right of Browser: {"note": "HTML is easy", "date": "2026-05-25T08:03:18.814Z"}
    Note right of Browser: Event listener captures the form submission and prevents default page reload
    Server->>Database : save note
    Database-->>Server : note saved
    Server-->>Browser: HTTP status code 201 response
    Note right of Browser: {"message": "note created"}

    Note right of Browser: note content added to page using Javascript
    Note right of Browser: No full page reload performed